{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "playground",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/playground/playground.ps1",
            "cwd": "${workspaceFolder}"
        }
    ]
}